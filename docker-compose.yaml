version: '3.9'

services:
  web:
    context: .
    name: todo-app:latest
    #image: trainwithshubham/node-app:latest
    ports:
      - "8000:8000"

version: '3.9'

services:
  web:
    build:
      context: .
    image: todo-app:latest
    ports:
      - "8000:8000"
